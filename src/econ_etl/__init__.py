"""econ_etl — a small, production-style ETL pipeline for macro-economic indicators.

Extracts indicators from the World Bank Open Data API, transforms them into a
tidy, validated long-format table, and loads them into a SQL database.
"""

__version__ = "0.1.0"
